# Core
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-dotenv==1.0.1

# LLM client (OpenAI-SDK-compatible; used to call Groq and, as fallback, Gemini's
# OpenAI-compatible endpoint — see app/llm_client.py)
openai==1.58.1

# Retrieval (Task 1 KB matching)
rank-bm25==0.2.2

# Bonus: Streamlit demo UI
streamlit==1.40.2

# Testing / eval harness
pytest==8.3.4
